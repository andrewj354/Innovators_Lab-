# Frontend Environment Variables
# Copy this file to .env and update values as needed

# API Gateway URL (default: /api for relative path, works with proxy)
# For production, set to http://localhost:8000/api or your deployed API URL
VITE_API_URL=/api

# Application settings
VITE_APP_NAME=Innovators Lab
VITE_DEBUG=false
